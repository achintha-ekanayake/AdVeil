name = "adveil-issue-proxy"
main = "worker.js"
compatibility_date = "2024-01-01"

# Create with: wrangler kv namespace create ISSUE_RATE_LIMIT_KV
# Then paste the returned id below.
kv_namespaces = [
  { binding = "ISSUE_RATE_LIMIT_KV", id = "f77ca5a848c548a6bb01e904ec0160c1" }
]

# Secrets are not stored in this file. Set separately with:
#   wrangler secret put GITHUB_TOKEN
